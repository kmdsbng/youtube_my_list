!!! XML
!!!
%html
  %header
    %title 再生リスト: #{@title}
  %body
    .main
      %h1 再生リスト: #{@title}
      %ol
      - @entries.each do |e|
        %li
          - if (e.href)
            %a{:href => "play?url=#{u e.href}"} #{e.title}
          - else
            %span= e.title


